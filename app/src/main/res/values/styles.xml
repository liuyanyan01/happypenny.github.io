<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AdTopic">
        <item name="android:background">@drawable/ad_topic_item_bg</item>
    </style>

    <style name="ADCardName">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:ellipsize">end</item>
        <item name="android:singleLine">true</item>
        <item name="android:textColor">#494b4c</item>
        <item name="android:textSize">16sp</item>
    </style>

    <style name="ADRatingBar" parent="@android:style/Widget.Holo.RatingBar.Small">
        <item name="android:progressDrawable">@drawable/ratingbar_progress_drawable</item>
    </style>

    <style name="ADDescription">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">#a2a2a2</item>
        <item name="android:textSize">13sp</item>
    </style>

    <style name="ADCardFreeBtn">
        <item name="android:background">@drawable/v2_toolbox_card_btn_bg</item>
        <item name="android:textColor">@android:color/background_light</item>
        <item name="android:textSize">17sp</item>
        <item name="android:singleLine">true</item>
        <item name="android:gravity">center</item>
    </style>

</resources>
